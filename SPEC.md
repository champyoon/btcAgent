# SPEC · BTC DCA Agent 상세 명세

갱신일: 2026-09-18 (KST). 현재 정책·구현 상태를 동기화했다.
서비스 개요는 [SERVICE.md](SERVICE.md), 실행은 [README.md](README.md), 검증 근거는 [round2_report.md](evaluation/round2_report.md)를 참고한다.
기존 §번호는 유지한다. 구현과 테스트 실측은 구분하며 이번 문서 갱신에서 테스트를 재실행하지 않았다.

## 0. 서비스 한 줄 정의

> 사용자가 설정한 월 예산으로, 매달 절반은 정액 매수하고 나머지 절반은 세 가지 매수 전략(하락일·
> 정기 분할·RSI) 중 선택한 방식으로 매수하도록 돕고, 과거 4년 데이터로 세 전략을 비교 시뮬레이션하며,
> 실제 매수·관망 기록과 예산 현황을 관리해주는 개인용 BTC DCA 에이전트.

## 1. 사용자 · 문제 · 가치

**누구를 위한 서비스인가**: 로그인 없이 개인 한 명의 설정과 기록을 관리하는 개인용 MVP. BTC를
매달 꾸준히 분할매수하고 싶지만, (a) 남은 예산이 얼마인지, (b) 정해둔 매수 조건이 지금 충족됐는지,
(c) 예전에 어떤 근거로 매수·관망했는지를 매번 손으로 챙기기 번거로운 사람.

**이 에이전트 없을 때는 어떻게 해결하고 있는가**: Upbit 앱에서 현재가만 보고, 이번 달 예산 중
얼마를 썼는지·지표가 조건에 맞는지·과거에 이 전략을 썼으면 어땠을지를 전부 직접 계산하거나 기억에
의존한다.

**이걸로 뭐가 좋아지는가**:
1. 월 예산·매수 기록·남은 예산을 자동으로 추적해 매번 손으로 계산하지 않아도 된다.
2. 세 전략의 매수 조건을 코드로 결정적으로 판정해, 조건 충족 여부를 감이 아니라 근거로 확인한다.
3. 전략을 바꾸기 전에 과거 4년 데이터로 "이 전략을 계속 썼다면 어땠을지"를 비교해볼 수 있다.
4. 매수든 관망이든 그 순간의 지표·근거가 기록에 남아 나중에 판단을 돌아볼 수 있다.
5. 투자 현황 집계에 영향을 주는 동작(매수 기록 생성·수정·취소)은 항상 승인을 거친다.

**서비스가 하지 않는 것** (역할의 경계):
- 실제 거래소 주문을 실행하지 않는다 — 사용자가 신고한 실제 매수 내역을 기록하며, 가상 시뮬레이션과 구분한다.
- 자동 알림을 보내지 않는다 — 사용자가 질문할 때만 답한다.
- 과거 성과가 가장 좋았던 전략을 자동으로 선택하지 않는다 — 시뮬레이션 결과는 참고 정보이고,
  전략 선택은 항상 사용자가 한다.

## 2. 최초 이용과 월별 흐름

### 2-0. 최초 이용

처음 이용 시 서비스 컨셉과 세 가지 매수 방식(§3)을 설명하고 월 투자금을 입력받는다. 월 200만
원은 예시일 뿐 고정값이 아니며, 사용자가 직접 월 투자금을 설정한다.

기본 계획: 매월 1일에 월 투자금의 절반을 매수하고, 나머지 절반은 사용자가 선택한 전략 방식으로
매수한다. 해당 월 계획이 시작된 사용자는 전략이나 과거 신호 유무와 관계없이 월말까지 실제 매수가 완료되지 않으면 월 마지막 날에 남은 예산을
매수한다.

**월중에 처음 이용한 경우 (정책 변경 — 사용자와 확정)**: 이전에는 무조건 다음 달
1일부터만 정식 계획을 시작할 수 있었다. 이제는 **이번 달 또는 다음 달 중 사용자가 확인한 달로**
시작할 수 있다 — 월중 이용자가 이번 달 예산의 남은 부분부터 바로 계획을 시작하고 싶어 하는 실제
요구를 반영했다. 사용자가 "9월부터 시작할게"/"이번 달 예산은 200만원"처럼 특정 월을 명시하면 그
의도를 반영하고, 아무 말도 하지 않았다면 **이번 달을 기본으로 제안한다**(과거에는 "오늘이 1일이
아니면 무조건 다음 달"이었던 기본값을 폐기). 과거 달로 소급해서 시작할 수는 없다 — 이번 달보다
이전 달을 요청하면 이번 달로 대체하고 그 사실을 안내한다(아래 "요청 월과 적용월이 다를 때" 참고).
이번 달을 선택하면 남은 예산 관리·전략 선택·매수 기록은 이번 달부터 바로 대상이 되고, 다음 달을
선택하면 이번 달은 여전히 서비스 설명·현재 시장 상태 조회·전략 시뮬레이션·월 예산 설정만 이용할 수
있다(기존 규칙 그대로).

**월 예산 설정도 §4-2의 제안→확인 절차를 거친다(2026-09-18 확정)** — 최초 설정이든 이후 변경이든
사용자가 확인하기 전까지는 실제로 저장되지 않는다. 확인 절차가 계산 결과를 몰래 바꾸지 않는다는
원칙은 그대로다 — "언제 반영할지"를 먼저 계산해 제안하고, 사용자가 그 제안에 동의해야 실제로
반영된다.

**이미 시작월이 설정된(아직 도래하지 않은) 계획을 앞당기는 경우**: 월중 이용자가 처음에는 "다음
달부터 시작할게"를 골랐다가, 이후 "역시 이번 달부터 시작하고 싶어"로 마음을 바꾸는 경우를 지원한다
— 아직 시작하지 않은 계획이라면(계획 시작월이 아직 도래하지 않음) 시작월을 이번 달로 앞당길 수
있다. 이는 §4의 일반 예산 변경(계획이 이미 시작된 뒤에는 항상 다음 달부터 적용)과는 다른 절차다 —
아직 시작 전인 계획의 시작월 자체를 바꾸는 것이지, 이미 진행 중인 계획의 예산을 바꾸는 게 아니다.
- 이번 달분 예산을 새로 받아 제안을 만든다(다음 달분과 다를 수 있다 — 다르면 확인 카드·최종 안내
  양쪽에서 "이번 달 예산"과 "기존에 설정된 다음 달 예산"을 각각 구분해서 보여준다).
- 기존에 설정돼 있던 다음 달 예산 항목은 **삭제하지도, 이번 달로 그대로 복사하지도 않는다** — 그
  달이 오면 그대로 적용된다. 앞당기기는 "새 시작월의 예산 항목을 하나 더 추가하는 것"이지 "기존
  설정을 지우는 것"이 아니다.
- 실제 매수 기록·이미 선택된 전략에는 손대지 않는다 — 이 기능이 바꾸는 건 계획 시작월과 예산
  이력뿐이다.
- 확인하기 전까지는 아무것도 바뀌지 않고, 취소하면 기존 다음 달 시작 계획이 그대로 유지된다(§4-2와
  같은 제안→확인→저장 원칙).

**요청 월과 적용월이 다를 때**: 사용자가 명시한 월과 실제로 적용 가능한 월(이번 달/다음 달 중
하나, 위 규칙대로)이 다르면 — 예를 들어 과거 달을 요청했거나 이번 달·다음 달이 아닌 먼 미래를
요청한 경우 — 조용히 다른 달로 바꿔 진행하지 않는다. 이유와 실제 적용될 달을 구조화된 확인
안내에 명시하고, 사용자가 그 대안을 확인해야만 저장한다. 이미 계획이 시작된 뒤의 일반 예산
변경(§4, 항상 다음 달부터)에서 요청 월이 다음 달이 아닐 때도 같은 방식으로 이유·대안을 안내한다
— 두 경우(최초 시작의 이번/다음 달 선택 vs 이미 시작된 계획의 항상-다음-달) 허용 범위가 다르므로
안내 문구도 구분한다.

> **필요한 API 스키마**: `set_monthly_budget`(§4-2) 도구가 `requested_year`/`requested_month`
> (둘 다 선택, 생략하면 특정 월을 요청하지 않은 것으로 처리)를 받는다. 서버는 현재 계획 상태(계획
> 없음/아직 시작 전/이미 시작됨)와 이 값을 함께 보고 위 세 동작(최초 설정/앞당기기/일반 변경) 중
> 하나를 자동으로 고른다 — 클라이언트가 어느 동작인지 직접 지정하지 않는다. `/query` 응답의
> `budget_change_needs_confirmation`에 `action`("initial"/"advance"/"change"),
> `previous_start_month`/`previous_start_amount`(앞당기기일 때만) 필드가 추가된다(§4-2 API 계약
> 참고).

### 2-1. 월별 흐름

1. 계속 이용하는 달에는 1일에 예산 절반을 직접 매수하고 기록한다(§4). 월중 첫 시작은 기존 기록을 확인하고, 기록이 없으면 첫 절반 매수 단계를 안내한다. 기록 부재를 실제 미매수로 단정하지 않으며 월말에는 잔여액을 중복 없이 안내한다.
2. 필요하면 과거 4년 시뮬레이션(§6)과 현재 시장 상태(§3-1)를 조회한다.
3. 이번 달 남은 예산(기본 계획은 절반, 실제 금액은 §4 기준)에 적용할 전략을 선택한다 — 선택 전에는 **'전략 미선택'** 상태로
   관리하며, 과거 성과나 다른 기준으로 자동 선택하지 않는다. 전략은 월별로 독립적으로 관리한다.
4. 이후 사용자가 질문할 때마다, 선택한 전략의 조건 충족 여부와 남은 예산을 안내한다.

### 2-2. 관망(hold) 기록 (기존 설계 유지)

지표 조회나 전략 조건 확인 후 "이번엔 매수하지 않고 지켜보겠다"는 결정도, 실제 매수와 마찬가지로
근거와 함께 기록할 수 있다. 세부 설계는 이전 검토에서 이미 확정했고 이번 갱신에서도 그대로 유지한다:

- **저장 항목**: 사용자 메모뿐 아니라 결정 시점의 개별 지표 값(RSI·MA200 괴리율·기간별 DD·MDD,
  §3-1)과 그 시점에 선택돼 있던 전략의 조건 충족 여부(§3)까지 함께 저장한다. 메모만 남기면 나중에
  "왜 관망했는지"를 되짚을 근거가 사라져 §1의 핵심 가치를 못 지킨다. DD·MDD는 계산 기준·버전
  식별자(`dd_mdd_calc_basis`)와 함께 저장해, 계산 정책이 바뀌어도 과거 기록과 구분할 수 있게
  한다(§6-2, 정책 변경).
- **도구·저장소**: 매수 기록과는 별도 도구 `record_watch_decision`을 두되, 저장 파일은 실제 매수
  기록(§4)과 같은 장부를 공유하고 각 항목에 `"type": "buy" | "watch"` 필드로 구분한다. 조회하면
  매수·관망 기록을 시간순으로 함께 볼 수 있다.
- **승인 필요 여부**: 관망 기록은 승인 없이 자동 기록한다(위험도: read). 자금 이동이 없는 결정이라
  사용자 확정 정책상 별도 승인을 생략한다. read 분류라도 실제 장부를 변경한다.

> **구현 현황**: 월 계획·시작월·예산·전략·관망 기록 구현. 이번 달 시작·시작월 앞당기기·확인·후속 입력을 지원한다.

### 2-3. 후속 입력 상태(awaiting_input) — 무상태 API에서 후속 답변 잇기 (실사용 신고 대응)

**문제**: "자 뭐부터 시작하면 돼?" → (에이전트가 월 예산 금액을 되물음) → 사용자가 "200만원"이라고만
답하면, `/query`는 매 요청이 독립적인 무상태 API라 그 답이 방금 물어본 질문에 대한 것인지 알 방법이
없다. "200만원"이라는 문장 자체에는 예산 설정을 나타내는 신호(반복 주기 단어·결정 어미·"예산"
키워드 등, §4-2 아래 "질문 의도 판별" 참고)가 전혀 없어, 아무 라우팅에도 안 걸리고 범위 밖으로
거절된다. `answer` 문자열에 "얼마"가 있는지 찾아 상태를 추정하는 방식은 채택하지 않는다 — 문구가
조금만 바뀌어도 깨지는 방식이기 때문이다.

**설계**: §4-2의 예산 확인 토큰, §5의 전략 확인 토큰과 같은 원리(서버가 발급한 토큰을 클라이언트가
다음 요청에 그대로 실어 보낸다)를 재사용하되, 이건 "제안을 확정 짓는" 토큰이 아니라 "다음 메시지가
방금 물어본 질문의 답이다"라는 표시일 뿐이라 별도 이름(`awaiting_input`)과 별도 저장소를 쓴다.

- Agent가 사용자에게 후속 정보(현재는 월 예산 금액 하나)를 물어야 할 때, 그 사실을 자유 서술이
  아니라 전용 도구(`request_monthly_budget_amount`, 무인자·무부작용)를 호출해 남긴다. 실제로
  사용자에게 보여줄 질문 문장은 Agent가 계속 자연어로 직접 작성한다 — 이 도구는 "곧 후속 질문을
  했다"는 서버 측 신호만 남긴다.
- `/query` 응답에 `awaiting_input: {kind: "monthly_budget_amount", awaiting_input_token}`이
  추가된다(없으면 필드 자체가 없다).
- 클라이언트는 바로 다음 `/query` 요청의 `awaiting_input_token` 필드에 그 값을 그대로 실어
  보낸다. 서버는 이 토큰이 유효하면(1회용 — 사용 여부와 무관하게 즉시 소진) 일반 라우팅보다 먼저
  이번 메시지에서 원화 금액("200만원"/"200만 원"/"2,000,000원")을 뽑아본다.
  - 금액을 찾으면: 일반 라우팅·LLM 호출 없이 곧바로 `propose_budget_change`를 호출해 예산
    제안을 만든다(§4-2와 완전히 같은 제안→확인 절차 — 이 경로로 들어와도 즉시 저장되지 않는다).
  - 금액을 못 찾으면(사용자가 다른 주제로 넘어감): 토큰은 이미 소진했으니 그대로 일반 라우팅으로
    진행한다 — 후속 입력 상태가 계속 남아 다음 메시지까지 오염시키지 않는다.
- 토큰을 생략하면(기존 Swagger 단독 질문과 완전히 동일) 항상 일반 라우팅만 탄다 — "200만원"만
  단독으로 보내면 예산 설정으로 해석되지 않고 그대로 범위 밖 질문 취급된다(맥락 없는 금액을
  임의로 예산 설정으로 해석하지 않는다는 원칙).
- 저장 위치는 §4-2/§10-1과 같은 이유로 서버 프로세스 메모리다(재시작하면 대기 상태가 사라지는
  것도 같은 종류의 알려진 한계).
- 브라우저(세션)마다 독립적으로 최근 토큰 하나만 들고 있으면 되므로, 세션 간 상태가 섞이는
  문제는 클라이언트 쪽 설계(세션별 상태 보관)로 자연히 해결된다 — 서버는 토큰 자체가 곧 자격
  증명이라 별도의 세션 개념을 두지 않는다.

> **구현 현황**: 위 흐름 전부 구현·검증 완료. LLM 호출 없이 토큰 발급·소진·재사용 방지·다른
> 주제 전환 시 해제를 확인했고, 실제 제출 모델로 "자 뭐부터 시작하면 돼?" → "200만원" 전체
> 흐름도 재현해 확인했다(REPORT.md 최신 절 참고).

**두 번째 종류 — `buy_execution_detail` (실사용 신고 #2 대응)**: "오늘 50만원어치
BTC 매수했어"처럼 금액·날짜는 있지만 체결 가격(`price_krw`)·수량(`quantity_btc`)이 모두 빠진
실제 매수 보고에도 같은 메커니즘을 재사용한다. 다만 `monthly_budget_amount`와 두 가지가 다르다:

- **별도 저장소·별도 kind 이름**: `_PENDING_AWAITING_INPUT[token] = {"kind":
  "buy_execution_detail", "amount_krw", "executed_date"}` — `monthly_budget_amount` 토큰과
  섞이지 않는다(다른 kind의 토큰으로는 이 흐름을 확정할 수 없다).
- **후속 입력을 받은 뒤 바로 실행하지 않는다**: `monthly_budget_amount`는 후속 금액을 받으면
  `propose_budget_change`(제안만)로 이어지지만, 이건 `record_virtual_buy`가 원래 요구하는
  §10-1 승인 절차로 이어져야 한다 — 서버가 1차 메시지의 금액·날짜와 2차 메시지의 가격·수량을
  직접 합쳐 `approvals.create()`로 승인 카드를 만든다(LLM이 두 메시지에 걸친 정보를 다시 정확히
  합치는 걸 기대하지 않음 — 금액이 둔갑하거나 현재 시세가 체결가로 둔갑할 위험을 원천 차단).
- 후속 메시지 파싱: 원화 표기("1억원"/"100,000,000원")면 `price_krw`, 단위가 붙은 숫자
  ("0.005개"/"0.005 BTC"/"0.005비트코인")면 `quantity_btc`로 본다 — 두 표기는 문법적으로
  겹치지 않는다.
- **토큰 발급이 프롬프트 준수에만 의존하지 않는다**: 시스템 프롬프트는 가격·수량이 빠졌을 때
  `request_buy_execution_detail`을 먼저 호출하라고 지시하지만, 실제 라이브 검증 중 Haiku
  4.5가 이 지시를 어기고 도구 호출 없이 말로만 되묻은 사례가 재현됐다 — 그러면 토큰이 발급되지
  않아 다음 턴의 답변을 연결할 방법이 없다. 그래서 서버가 매 턴 끝에 안전망을 하나 더 둔다:
  ledger_agent가 매칭되고, 이번 턴에 승인 요청도 awaiting_input도 전혀 만들어지지 않았고,
  질문이 과거형 매수 보고 키워드(매수했어/샀어/구매했어)를 포함하며, 원화 금액이 정확히 1건만
  있고, 수량 표기나 부정 표현("아직 안 샀어" 등)이 전혀 없으면 — 서버가 `request_
  buy_execution_detail`을 직접 호출해 토큰을 강제로 발급한다. 금액이 이미 2건 이상이거나
  수량까지 이미 있는 문장(정보가 충분해 모델이 직접 `record_virtual_buy`를 호출해야 할 상황)은
  이 안전망 대상이 아니다 — 애매한 문장을 서버가 임의로 해석하지 않는다는 원칙을 그대로 지킨다.

## 3. 세 가지 매수 방식

모든 방식의 첫 매수는 매월 1일, 월 투자금의 절반이다. 나머지 절반에 아래 세 방식 중 하나를 적용한다.

### ① 하락일 매수

다음 **두 조건을 모두** 충족하면 매수 조건 충족으로 안내한다. 가상 체결은 §6의 다음 날 시가 규칙을 따른다:
- 확정 일봉 종가 < 해당 월 첫 실제 매수의 평균 체결가
- (일봉 종가 / 일봉 시가 − 1) × 100 ≤ −5%

> **혼동 주의**: "전일 종가 대비 하락률"이 아니다(당일 시가 대비 당일 종가 하락률). "첫 매수가
> 대비 −5%"도 아니다(각 조건은 서로 다른 기준값과 비교하는 독립된 두 조건이며 AND로 결합한다).
> 시뮬레이션(§6)에서는 "해당 월 첫 실제 매수의 평균 체결가" 대신 **해당 월 첫 가상 매수의 체결가**를
> 비교 기준으로 쓴다.

### ② 정기 분할

- 두 번째 매수 예정일은 **매월 15일**.
- 전통적인 DCA(정액·정기 분할매수) 방식 그대로다.

### ③ RSI 매수

- 확정 일봉 기준 **RSI(14) ≤ 30**일 때 매수한다.
- 첫 매수가보다 낮아야 한다는 조건은 없다 — RSI 조건만 본다.
- "RSI ≤ 30"은 이 서비스의 **확정된 매수 규칙**이다. RAG 문서(§8)에서 일반적인 "RSI 과매도"
  설명과 이 서비스의 매수 규칙을 반드시 구분해서 설명해야 한다(용어집의 일반 설명 ≠ 매수 트리거).

실제 이용에서는 **해당 월의 계획이 시작된 사용자에 한해**, 과거 신호·전략 선택 여부와 관계없이 월말에 남은 예산을 **월 마지막 날에** 매수하도록 안내한다. 백테스트에서는 두 번째 가상 매수가 없으면 마지막 날 시가로 잔액을 한 번만 매수한다.

> **구현 현황**: src/strategy.py의 세 전략 조건을 월 상태·백테스트 도구에 연결했다. 종합 매수 등급은 사용하지 않는다.

### 3-1. 개별 지표 설명 (종합 판정표는 폐기 확정)

**확정**: 기존에 검토했던 "RSI·200일 이평 괴리율·1년 드로다운을 종합해 충족/미충족/혼재/판정 불가로
보여주는 판정표"(`assess_dca_signal`)는 **폐기한다.** 임계값·조합 규칙([TBD]였던 항목들)도 더 이상
정할 필요가 없다 — 세 전략(§3)이 각자 명확한 조건으로 매수를 결정하므로, 이런 별도의 종합 판정
게이트가 필요 없다.

**유지**: RSI·MA200 괴리율·기간별 DD·MDD는 **값과 그 의미를 함께 설명한다.** "지금 BTC 지표
요약해줘" 같은 질문에는 아래를 종합 등급 없이 안내한다:
- **RSI(14일)** — 현재 값(계산법: RMA, §8·§14 참고)과 함께 "70 초과는 과매수, 30 미만은
  과매도" 같은 해석 기준을 설명한다. 예: "RSI 42 — 중립 구간(과매수·과매도 어느 쪽도 아님)".
- **200일 이동평균 괴리율** — 현재 값과 함께 "최신 확정 종가가 200일 종가 평균보다 얼마나 높거나 낮은지"를 설명한다.
- **기간별 DD(현재 하락률)·MDD(최대 낙폭)(최근 30일/365일/48개월, 정책 변경 — 종가
  기준으로 통일하고 MDD를 새로 추가)** — DD는 "그 기간 최고 종가 대비 지금 얼마나 빠졌는지",
  MDD는 "그 기간 중 어느 시점에서든 발생했던 가장 큰 고점 대비 하락폭"으로 서로 구분해
  설명한다(§6-2·§7). 이 값들은 새로운 매수 조건이나 종합 점수로 연결하지 않는다.

**만들지 않는 것**: 위 세 지표를 하나로 합쳐 "충족/미충족/혼재/판정 불가" 같은 **종합 매수 등급**을
내는 것은 하지 않는다 — 그 역할은 §3의 전략별 조건이 대신한다.

**전략③(RSI 매수)의 조건 판정과는 구분**: 이 절의 RSI 설명은 순수 정보 제공이고, "RSI ≤ 30이면
매수 조건 충족으로 안내한다"는 §3 전략③의 결정 규칙은 **그대로 유지**된다 — 같은 RSI 값을 (a) 여기서는
설명하고 (b) 전략③에서는 그 값이 매수 조건 판정에 쓰이는 것이며, 서로 다른 역할이다.

## 4. 실제 예산과 기록

**남은 예산** = 월 설정 예산 − 해당 월 유효한 실제 매수금액 합계.

예: 월 예산 200만 원, 첫 실제 매수 120만 원 → 남은 예산 80만 원.

- 예산 이상을 매수했다면 실제 기록은 그대로 보존하고 "예산 충족·초과"로 안내한다. 추가 매수 안내
  금액은 0원으로 처리한다.
- 월 예산 변경은 **다음 달부터** 적용한다. 이번 달은 기존 예산을 유지한다.
- **월 예산 설정·변경은 §4-2의 제안→확인 절차를 거쳐야 실제로 반영된다(2026-09-18 확정)** — 위
  "다음 달부터 적용"이라는 규칙 자체는 그대로지만, 그 적용이 사용자 확인 이전에 미리 저장되지는
  않는다.

**기록 입력**: 실제 매수 날짜·금액·BTC 수량 또는 평균 체결가를 입력받는다. 부족한 정보는 되묻고,
기록 내용을 사용자에게 확인받은 뒤 저장한다. **금액·체결가로 역산한 수량**과 **사용자가 직접 입력한
실제 수량**은 서로 다른 값일 수 있으므로 구분해서 저장한다(반올림·수수료 등으로 차이가 날 수 있음).

**평균 매수가** = 총 유효 매수금액 / 총 유효 매수 BTC 수량. 기존 보유분을 입력하지 않았다면
"서비스에 기록된 매수분 기준"이라고 표시한다. 수수료는 이번 MVP 계산에서 제외한다.

**매수 기록 수정·취소**:
- 대상 기록과 변경 전·후 내용을 사용자에게 확인받은 뒤 반영한다.
- 원본과 변경 이력을 보관한다(덮어쓰지 않음).
- 취소된 기록은 집계(평균 매수가·남은 예산)에서 제외한다.
- 변경·취소 후에는 평균 매수가와 월 남은 예산을 재계산한다.
- 이건 **장부 정정**이며 실제 거래 취소가 아님을 응답에 명시한다.
- **승인 필요**: 수정·취소도 §10-1의 승인 ID 설계로 처리한다 — write/destructive 성격이 매수
  기록 생성과 같으므로 같은 게이트를 통과해야 한다. 도구명·스키마는 §4-1에서 확정.

### 4-1. 기록 스키마·수정·취소 도구 설계 확정

기존 `ledger.json` 항목엔 안정적인 식별자가 없어(타임스탬프뿐) 특정 기록을 지목해 수정·취소할
방법이 없었다. 아래로 확정한다.

**장부 항목 스키마 확장** (`data/ledger.json`, 기존 필드 유지 + 추가):
```json
{
  "record_id": "b_0001",
  "type": "buy",
  "amount_krw": 1200000.0,
  "price_krw": 106000000.0,
  "quantity_btc": null,
  "note": "9월 정기 매수",
  "executed_at": "2026-09-01T09:00:00+09:00",
  "executed_date": "2026-09-01",
  "execution_time_precision": "datetime",
  "recorded_at": "2026-09-16T00:00:00+00:00",
  "status": "active",
  "history": []
}
```
- `record_id`: 생성 시 서버가 발급(순번 기반 `b_0001`/`w_0001` 등 — buy/watch 접두사로 구분).
  수정·취소 시 이 id로 대상을 지목한다.
- `executed_date`: 실제 매수의 KST 날짜. 월별 예산 집계에 사용한다.
- `executed_at`: 실제 매수 시각(시간대 포함). 입력·저장 시각인 `recorded_at`과 구분한다.
- `execution_time_precision`: `date | datetime`. 날짜만 알면 `date`와 `executed_date`를 저장하고 `executed_at=null`로 둔다. 시각을 알면 `datetime`으로 저장하고 KST로 환산한 날짜와 `executed_date`가 일치해야 한다. 날짜 정정 시 두 필드를 함께 검증하며 임의의 체결 시각은 만들지 않는다.
- 금액·가격·수량은 유한한 양수여야 한다. 수량과 가격을 함께 받으면 불일치를 확인하고, 수량 기반 집계와 표시 가격이 어긋나지 않도록 한다. 유효한 매수가 없으면 평균 매수가는 계산 불가로 표시한다.
- `quantity_btc`: 사용자가 실제 수량을 직접 입력했을 때만 채운다. 금액/체결가로 역산한 수량과
  구분하기 위해 기본값은 `null`(계산은 그때그때 amount_krw/price_krw로 대체 수행).
- `status`: `"active" | "cancelled"`. 집계는 `type=buy`이면서 `status=active`인 기록만 합산한다(관망 기록 제외).
- `history`: 수정 전 스냅샷을 쌓아둔다(수정할 때마다 직전 상태를 이 배열에 추가) — 원본을
  덮어쓰지 않는다는 원칙(§4)을 이렇게 구현한다.

**수정 입력 원칙**: 실제 매수 날짜도 정정할 수 있어야 한다. 수정 시 미전달 필드는 유지하고, 수량을 명시적으로 비우는 요청은 역산 수량으로 전환하는 요청과 구분해 확인한다. 수정·취소 이력에는 이전 상태·변경 시각·사유를 보존한다. 아래 시그니처는 역할 설명이며 실제 요청 스키마는 필드 누락과 명시적 null을 구분해야 한다.

**신규 도구**:
- `amend_virtual_buy(record_id: str, amount_krw: float | None, price_krw: float | None, quantity_btc: float | None, executed_date: str | None, executed_at: str | None, execution_time_precision: str | None, note: str | None, reason: str)`
  — 지정한 필드만 갱신(미전달 필드는 기존값 유지), 변경 전 상태를 `history`에 추가. 위험도:
  write(승인 필요).
- `cancel_virtual_buy(record_id: str, reason: str)` — `status`를 `"cancelled"`로 바꾼다(레코드
  삭제 아님). 위험도: write(승인 필요) — 되돌릴 수 없는 삭제가 아니라 상태 전환이라
  `reset_ledger`(destructive)보다는 한 단계 낮은 write로 분류한다.
- 둘 다 §10-1의 승인 ID 설계를 그대로 따른다 — `/query`가 승인 대기 항목으로 제안하면
  `approval_id`가 발급되고, `/approve`는 그 id만으로 실행한다.

**과거 매수 신호나 시뮬레이션 결과를 실제 매수 기록으로 저장하면 안 된다** — 실제 기록(§4)과 가상
시뮬레이션 결과(§6)는 항상 분리해서 보관한다.

> **구현 현황**: src/ledger.py에서 월별 집계·수량·평균 매수가·매수 기록 수정/취소 이력을 지원한다. 매수 변경은 승인 후 실행한다.

### 4-2. 월 예산 설정·변경 확인 설계 확정 (2026-09-18 기획 변경)

**변경 배경**: `set_monthly_budget`은 원래 승인·확인 절차 없이 호출 즉시 반영됐다(§14-0에서
"read(자금 이동 없는 설정값 변경)"로 분류했던 이유). 실사용 검증 중 이 즉시 반영 방식이 실제
위험 표면이 된다는 지적을 받았다 — 예를 들어 "매달 200만원씩 사는 예시를 설명해줘"처럼 설명을
요청하는 질문과 "매달 200만원씩 투자할래"처럼 실제로 설정하려는 지시를 LLM이 잘못 구분하면, 확인
절차가 전혀 없으니 그 자리에서 바로 잘못된 예산이 반영된다. **§5의 전략 변경(제안→확인)과 같은
구조로 통일**하기로 확정한다.

**흐름**:
1. 사용자가 예산 설정/변경 의도를 밝히면, 서버는 **제안만 만들고 실제로 반영하지 않는다** — 현재
   계획 상태를 보고 아래 세 동작(`action`) 중 하나를 자동으로 고르고, 월 예산 금액·적용 연월과
   함께 응답에 담는다(정책 변경 — §2-0 참고):
   - **`initial`**(계획이 아예 없음): 이번 달 또는 다음 달 중 사용자가 확인한 달로 시작한다.
     특정 월을 요청하지 않았으면 이번 달을 기본으로 제안한다.
   - **`advance`**(계획은 있지만 아직 시작 전이고, 사용자가 정확히 이번 달을 요청함): 아직
     시작하지 않은 계획의 시작월을 이번 달로 앞당긴다. 기존에 설정된 미래 시작월의 예산은
     삭제·복사하지 않고 그대로 유지하고, 이번 달분 예산만 새로 추가한다.
   - **`change`**(그 외 전부 — 계획이 이미 시작됐거나, 아직 시작 전이지만 이번 달로 당기라는
     요청이 아닌 경우): 기존 §4 규칙 그대로 **항상 다음 달부터** 적용한다.
2. 사용자가 동의하면 확인 API로 실제 반영한다. 동의하지 않거나 응답이 없으면 아무 것도 바뀌지
   않는다(제안은 그냥 소멸한다 — 별도 되돌리기 불필요).
3. `change`는 이번 달 예산과 과거 매수 기록을 건드리지 않는다. `advance`는 이미 설정된 미래
   시작월의 예산·전략에도 손대지 않는다 — 새 시작월(이번 달) 예산 항목만 추가한다.

**API 계약** (§10-1의 승인 ID 설계와 별개 저장소를 쓴다 — §5의 `confirm_strategy_change`와 같은
이유: 자금 이동이 없는 설정값 변경이라 일반 write 승인 대상은 아니지만, 서버 검증 없이 자연어만
믿고 반영해서는 안 되는 종류의 변경이다):
- `/query` 응답에 `budget_change_needs_confirmation: {confirmation_token, amount_krw,
  effective_month, is_initial, requested_month, month_mismatch, action, previous_start_month,
  previous_start_amount}` 추가. `is_initial`은 하위 호환용으로 남긴다(`action == "initial"`과
  동일). `action`/`requested_month`/`month_mismatch`는 추가 — 아래 "요청 월과 적용월이
  다를 때" 참고. `previous_start_month`/`previous_start_amount`는 `action == "advance"`일 때만
  채워진다(§2-0의 "앞당기기" 참고) — 그 외에는 `null`이다.
- `POST /confirm_budget_change {"confirmation_token": "..."}` — 제안을 실제로 반영한다. **서버가
  제안 시점에 저장해둔 금액·적용월만 쓰고, 클라이언트가 이 요청에 금액이나 월을 함께 보내도
  신뢰하지 않는다**(애초에 그런 필드를 받지도 않는다).
- `POST /cancel_budget_change {"confirmation_token": "..."}` — 제안을 취소한다. 아무 것도
  반영되지 않는다.
- 오류 계약: 존재한 적 없는 토큰은 404, 이미 확인·취소 처리된 토큰이나 아래 "낡은 제안" 판정에
  걸린 토큰은 409로 구분한다(§10-1의 승인ID 404/409 구분과 같은 원칙). 토큰은 1회용이며, 확인·
  취소 어느 쪽으로 소진돼도 재사용할 수 없다. 전략 변경용 토큰으로 예산 변경을 확인하거나 그
  반대로 시도해도 서로 다른 저장소를 쓰므로 항상 "존재하지 않는 토큰"으로 거부된다.
- 저장 위치: §10-1과 같은 이유로 서버 프로세스 메모리 — 재시작하면 대기 중인 제안은 사라진다
  (알려진 한계, §10-1과 동일하게 남긴다).
- **동시성**: 확인·취소가 동시에 들어오는 경합, 확인 두 번이 동시에 들어오는 경합 모두 명시적
  락(`threading.Lock`)으로 정확히 하나만 성공하도록 한다 — approvals.py가 GIL만 믿다가 실제
  동시 실행 검증에서 문제가 될 뻔했던 사례(§10-1 구현 이력)를 반복하지 않는다.
- **낡은 제안(stale proposal) 판정**: 제안을 만든 뒤 확인하기 전에 (a) 월 경계를 넘어 "동작
  종류(action)"나 "적용월" 계산이 달라졌거나, (b) 다른 요청으로 계획 상태(시작 여부·예산 이력)가
  바뀌었다면, 그 제안은 더 이상 유효하지 않다 — confirm 시점에 이 조건들을 다시 확인해 하나라도
  달라졌으면 confirm을 거부하고(409) 다시 제안하라고 안내한다. 제안 시점의 금액·적용월을 몰래 새
  상황에 맞게 바꿔서 실행하지 않는다. `advance` 제안도 예외 없이 같은 재확인을 거친다 — 예를 들어
  다른 제안이 먼저 확정돼 계획 상태가 바뀌었다면 낡은 `advance` 제안은 거부된다.

**무상태 API에서의 대상 명확화**: 이 API는 대화 문맥을 유지하지 않으므로, "네"·"적용해줘" 같은
자연어만으로는 어떤 제안을 확인하는 건지 알 수 없다(§5의 전략 변경 확인과 같은 문제). 반드시
`budget_change_needs_confirmation.confirmation_token`을 확인 API로 구조화해서 보내야 한다. 지금은
Swagger에서 이 토큰을 복사해 전달하는 절차로 검증하며, 이후 채팅 UI에서는 [적용]/[취소] 버튼에
이 API를 그대로 연결할 수 있다(이번 범위는 API까지 — UI 구현은 포함하지 않는다).

**질문 의도 판별**(도구 호출 여부만으로 확인, 확인 API 계약과는 별개 층위 — 라우팅·의도 판별의
정확성은 그 자체로 따로 검증해야 한다): 실제 설정/변경 지시("매달 200만원씩 투자할래")에는 제안을
만들고, 예시·설명 요청("매달 200만원씩 사는 예시를 설명해줘")에는 제안 없이 설명만 하며 설명 자체를
생략하지 않는다. 조회·계산 질문("BTC 1만원이면 얼마나 살 수 있어?")은 예산 도구를 아예 건드리지
않는다. BTC가 아닌 다른 자산 금액("이더리움에 매달 50만원 투자할래")은 그 금액으로 BTC 예산을
등록하지 않고 이 서비스가 BTC 전용임을 안내한다.

**요청 월과 적용월이 다를 때 — 이유·대안 명시 **: 사용자가 명시한 월이 실제 적용
가능한 달과 다르면(§2-0의 허용 범위 — `initial`은 이번 달/다음 달 중 하나, `change`는 항상 다음
달), 정책 자체를 바꾸거나 사용자가 말한 달로 소급 적용하지 않는다 — 대신 그 사실과 이유를
명시적으로 안내한다. `set_monthly_budget` 도구는 `requested_year`/`requested_month`를 받아(사용자가
특정 월을 언급하지 않았으면 생략) 제안 계산에 함께 넘기고, 서버는 `requested_month`(요청 월)와
`effective_month`(실제 적용월)를 비교해 다르면 `budget_change_needs_confirmation.month_mismatch`를
`true`로 채운다. 이 사실은 **자유 서술이 아니라 구조화된 최종 안내 블록에 직접 포함**한다 — 예산
제안이 있으면 plan_agent의 자유 서술 자체가 최종 답변에서 빠지므로(위 "무상태 API에서의 대상
명확화" 절 및 REPORT.md §15-7), 이유를 자유 서술에만 맡기면 통째로 사라진다. `action`에 따라 안내
문구를 구분한다 — `initial`(예: "요청하신 8월에는 시작할 수 없습니다 — 최초 시작은 이번 달 또는
다음 달만 가능합니다. 대신 9월부터 시작하는 제안입니다")과 `change`(예: "요청하신 9월에는 적용할
수 없습니다 — 월 예산 변경은 정책상 다음 달부터만 적용됩니다. 대신 10월부터 적용하는 제안입니다")
는 허용 범위가 서로 달라 같은 문구를 쓰면 안 된다. 사용자가 확인하기 전에는 이 대안 제안도 저장되지
않는다(기존 제안→확인 절차 그대로).

**narrative 필드 — 설명과 확인 안내를 구조적으로 분리 (실사용 UI 신고 대응)**: `answer`는
지금까지와 동일하게 Agent의 자유 서술 + 승인/전략변경/예산변경 구조화 안내(confirmation_token·
API 경로 문구 포함)를 전부 담아 하위 호환을 유지한다. 채팅 UI처럼 그 안내를 별도의 확인/취소
버튼으로 이미 보여주는 클라이언트는 같은 토큰·경로 문구가 화면에 중복 노출되는 문제가 있어,
`/query` 응답에 `narrative`를 추가한다 — `answer`에서 구조화 안내 블록만 뺀, 순수 설명 텍스트다
(예산 제안이 있을 때 plan_agent 문구가 빠지는 규칙은 `narrative`에도 동일하게 적용된다). `answer`를
정규식으로 잘라내 만드는 게 아니라, 서버가 애초에 두 필드를 각각 만들어 내려준다 — Swagger 등
카드가 없는 클라이언트는 `answer`만으로 계속 전부 확인할 수 있다.

> **구현 현황**: 위 흐름·API 계약 전부 이번에 구현·검증 완료(REPORT.md 최신 절 참고). 승인
> 실행/거절 후 재사용 차단, 확인·취소 경합, 낡은 제안 재확인까지 실제 스레드 경합과 실 `/query`→
> 확인 API→상태 조회 종단 시나리오로 확인했다. 월 불일치 안내·narrative 분리도 실제 제출 모델로
> 재현·확인했다. `initial`(이번 달 기본 제안·명시적 다음 달 선택 모두)/`advance`(기존 다음 달
> 예산 보존 확인 포함)/`change`(기존 정책 유지) 세 동작 모두 실제 제출 모델(Haiku 4.5, `global.`
> 프로파일)로 재현·확인했다.

## 5. 월중 전략 변경과 놓친 신호

**전략 변경 문의 시**:
1. 문의 시점 기준 **§7-1의 확정성 원칙(KST 09:00 경계 기준 마감된 최신 일봉까지)**으로 직전 달까지
   완료된 48개월을 재시뮬레이션한다(§6).
2. 현재 시장 상태(§3-1)·현재 선택 전략·이번 달 실제 매수금액·남은 예산을 안내한다.
3. 변경 여부와 변경 대상 전략을 사용자에게 확인받는다.
4. **확정 이후부터** 새 전략을 적용한다 — 과거 거래 기록을 새 전략에 맞춰 소급 변경하지 않는다.
5. 변경 전·후 전략, 변경 시점, 데이터 기준일을 기록한다.

**15일이 지난 뒤 전략 변경**:
- **마감 시각 확정: 15일 09:00(KST)** — 이 시각 이후에는 이번 달 **정기 분할** 방식으로 변경할
  수 없다(두 번째 매수 예정일 15일이 이미 지났으므로). 안내 문구로 명시한다.
- 시뮬레이션(참고용 비교)에서는 여전히 세 방식 모두 비교할 수 있다.
- 이번 달 실제로 선택 가능한 조건 전략은 **하락일·RSI** 두 가지뿐이다.
- 정기 분할 방식은 **다음 달**부터 다시 선택 가능하다.

**월말 처리**:
- **적용 대상**: 해당 월의 계획이 시작된 사용자에게만 적용한다. 다음 달 시작 예정인 사용자에게는 이번 달 월말 매수를 안내하지 않고, 설명·시뮬레이션·시장 조회와 다음 달 예산 설정을 제공한다.
- **마감 시각 확정: 해당 월 마지막 날 00:00(KST)** — 이 시각부터 이번 달 세 전략의 신규 선택·변경을
  마감한다.
- 남은 예산을 해당 월 마지막 날에 매수하도록 안내한다. 이미 예산을 채웠다면 매수 완료를 안내한다.
- 월말 대체 매수를 "세 전략의 지표가 모두 미충족"이라는 뜻으로 설명하지 않는다 — 월말 매수는
  조건과 무관하게 예산을 소진하기 위한 절차이지, 지표 판정 결과가 아니다.

**놓친 신호 예시** (10일 조건 충족 → 사용자가 13일에 질문):
1. 사용자가 말한 10일 신호를 확정 일봉으로 검증하고 발생 여부를 안내한다. 사용자 주장을 그대로 사실로 취급하지 않는다.
2. **조회 시점에 마감된 최신 일봉**으로 현재 조건 충족 여부를 다시 계산한다(13일 09:00 이전에는 11일 일봉까지, 09:00부터는 12일 일봉까지 사용) — 10일의 과거
   신호만으로 "지금도 충족 상태"라고 판단하지 않는다.
3. 실제 매수 기록을 바탕으로 남은 예산을 안내한다.

> **구현 현황**: 전략 제안·확인/취소·시간 경계·evaluate_current_condition 도구 연결을 구현했다. 실제 모델 검증 범위는 §14에서 구분한다.

## 6. 시뮬레이션 (백테스트)

- 과거 4년의 **동일한 구간**으로 세 전략을 비교한다.
- **동일한 사용자 월 예산**과 공통 계산 조건을 세 전략에 똑같이 적용한다.
- 조건 충족 판단에는 **확정된 데이터만** 사용한다(§7-1과 동일 원칙).
- 가상 매수는 **해당 매수일 일봉 시가**로 체결한다(확정) — 조건부 매수(하락일·RSI)는 "조건 충족
  다음 날"이 그 매수일이고, 정기 분할(매월 15일)·월말 대체 매수는 그 예정일 자체가 매수일이다.
  이 둘을 "해당 매수일의 일봉 시가"라는 하나의 규칙으로 통일한다.
- 미래 데이터를 참조하지 않는다(각 시점에서 그 시점까지의 데이터로만 판단).
- 지표 계산에 필요한 사전 데이터(RSI는 §6-4에 따라 최소 200일 선행 구간)를 시뮬레이션 시작 시점 이전에 미리
  확보해둔다.
- **수수료·슬리피지 모두 0으로 가정한다(확정)**.
- 실제 체결 기록(§4)과 가상 시뮬레이션 결과는 항상 분리해서 보관·표시한다.

**결과 항목**: 총 투자금, 누적 BTC 수량, 평균 매수가, 동일 평가 시점 기준 평가금액·손익·
수익률, 조건 매수 및 월말 대체 매수 횟수.

**주의**: 누적 BTC 수량 극대화를 모든 사용자의 최우선 목표로 고정하지 않는다(사용자 목표는 다양할
수 있음). 가상 시뮬레이션의 누적 수량과 실제 보유량을 혼동해서 표시하지 않는다.

**백테스트 구간 확정**: **직전 달까지 완료된 48개월**을 대상으로 한다 — "오늘로부터 롤링 4년"이
아니라 달력상 완결된 달 단위로 정확히 48개월을 쓴다. 진행 중인(이번) 달은 아직 완료되지 않았으므로
백테스트 대상에서 **아예 제외**한다(부분 포함하지 않음). 이걸로 "4년 구간 정의"와 "진행 중인 달
처리" 두 미확정 사항이 모두 해결됐다.

> **구현 현황**: src/backtest.py에서 확정 일봉으로 완료된 48개월을 비교한다. 실제 계획·장부를 가상 거래로 변경하지 않는다.

### 6-1. 계산·시간 경계의 구현 기준

- 일봉 날짜는 KST 09:00 시작일로 표기한다. 해당 일봉은 다음 날 09:00에 확정된다.
- 첫 가상 매수는 1일 시가에 시행한다. 이후 해당 월 1일 일봉부터 조건을 평가해 최초 충족 신호 다음 날에 남은 절반을 한 번 매수한다. 이전 달 신호로 1일에 두 번 매수하지 않는다.
- 월말 조건 매수와 의무 매수가 겹치면 잔액을 한 번만 매수한다. 최종 평가 가격은 마지막 대상 일봉의 확정 종가이며 세 전략에 동일하게 적용한다.
- 월초 1일 09:00 이전에는 직전 달 마지막 일봉이 아직 미확정이다. 직전 달까지의 48개월 결과를 완성된 결과로 만들지 않고 마감 후 재조회를 안내한다.
- 최신 데이터 지연에 대한 동의는 과거 구간 누락이나 지표 계산 기간 부족을 허용하지 않는다. 필요한 일봉이 없으면 해당 계산을 불가로 표시한다.
- 캐시는 48개월 비교 구간에 더해 지표의 선행 데이터를 확보한다. RMA는 동일한 초기화 기준과 누적 이력을 사용하며 매번 최근 14일만 잘라 재초기화하지 않는다. MA200에는 200개 유효 종가가 필요하다.
- 지표 값에는 기준 일봉과 계산 기간을 붙인다. 실시간 현재가와 확정 종가 기반 지표를 혼합하지 않는다.
- 드로다운의 고점·기간 기준은 §6-2로 확정한다. RSI 특수값 처리는 §6-3으로 확정한다. RMA 초기화는 §6-4로 확정한다. 계산 정밀도와 화면 표시는 §6-5로 확정한다. 확정된 전략 임계값을 변경하는 근거로 사용하지 않는다.

### 6-2. DD(현재 하락률)·MDD(최대 낙폭) 계산 기준 (정책 변경)

**기준 확정 — 종가(close)로 통일**: DD·MDD 둘 다 해당 기간에 포함된 **마감 일봉의 종가(close)**만
사용한다. 이전에는 DD를 고가(high) 기준으로 계산했다 — **그 기준은 폐기됐다.** 현재 가격 기준은
실시간 시세가 아니라 최신 확정 일봉의 종가다. 표시·설명에서 "최고가"라고만 부르지 않고 "최고
종가"라고 정확히 표시한다.

**DD(현재 하락률)**: 그 기간 최고 종가 대비, 지금(최신 확정 종가) 얼마나 빠져 있는지.

`DD(%) = (최신 확정 종가 / 기간 내 최고 종가 - 1) × 100`

예: 최고 종가 100,000,000원, 최신 확정 종가 80,000,000원 → -20%.

**MDD(최대 낙폭)**: 그 기간 **중 어느 시점에서든** 발생했던 가장 큰 고점 대비 하락폭 — DD와는
다른 개념이다(지금 가격이 이미 반등해 있어도 MDD는 그 시점의 낙폭을 그대로 반영한다). 날짜순으로
종가를 순회하며 그 시점까지의 최고 종가(러닝 최고치)를 계산하고, 각 날짜의
`(그날 종가 / 그 시점까지 최고 종가 - 1) × 100` 중 최솟값을 MDD로 한다 — **반드시 고점 이후에
발생한 저점만 낙폭 계산에 반영**하며, 단순히 기간 전체 최저가/최고가로 계산하지 않는다.

예: 종가가 100 → 60 → 90으로 움직이면 DD = -10%(최신 종가 90 / 최고 종가 100 - 1), MDD = -40%
(고점 100 이후 저점 60까지의 낙폭이 구간 중 가장 큼).

- DD·MDD 둘 다 0 이하의 부호 있는 백분율로 반환한다. 상승만 한 구간·횡보 구간의 MDD는 0%.
- 항상 `MDD ≤ DD ≤ 0`이다 — DD는 구간의 마지막 날에 같은 방식(그 시점까지의 러닝 최고 종가
  대비)으로 계산한 값이므로, 구간 전체에서 가장 낮았던 낙폭(MDD)보다 얕거나 같을 수밖에 없다.
- 1개월·1년·4년(정확히는 30일·365일·48개월) 값을 시장 설명용으로 함께 제공하며, 세 전략의 매수
  조건이나 새로운 종합 점수에는 연결하지 않는다.

**기간 확정**: 최신 확정 일봉의 시작일(KST 날짜)을 D로 두며, 모든 구간에 D를 포함한다.
- 30일(1개월 표기): D-29일부터 D까지(양 끝 포함). 표시에 "30일 기준"을 붙인다.
- 365일(1년 표기): D-364일부터 D까지(양 끝 포함).
- 48개월(4년 표기): D에서 달력상 48개월을 뺀 날짜는 제외하고(그 다음 날부터) D까지 포함한다.
  대응 날짜가 없으면 해당 월 말일로 보정한다(예: 4년 전 2월이 그 해엔 윤년이 아니라 29일이
  없으면 28일로 보정).
- **각 기간의 시작점부터 최고 종가를 새로 계산한다** — 기간 이전의 고점을 그 기간의 DD·MDD에
  끌어오지 않는다.
- 각 결과에 실제 구간 시작일·종료일, DD의 기준 최고 종가와 그 발생일, MDD의 고점·저점 각각의
  종가와 발생일을 표시한다. 누락 일봉 때문에 구간을 더 과거로 늘리거나 기간 부족을 임의로 채우지
  않는다.
- **한 기간의 데이터 부족(또는 결측)이 다른 기간이나 RSI의 데이터 부족과 일괄 처리되지 않는다** —
  예를 들어 확정 일봉이 100일뿐이면 RSI(200일 필요)는 계산 불가지만 30일 DD·MDD는 그대로
  계산된다. 지표별로 필요한 데이터가 실제로 확보됐는지 각자 판단한다.
- 48개월 DD·MDD는 최신 확정 일봉까지의 **이동 구간**이다. 직전 달까지 완료된 48개월을 사용하는
  백테스트와 종료일·목적이 다르다.
- DD·MDD는 과거 가격을 요약한 참고 정보일 뿐, 미래의 최대 하락폭이나 바닥을 예측하지 않는다.
  "과거 MDD까지 아직 하락 여유가 있다"는 식으로 서술하지 않는다.

**과거 스냅샷과의 계산 기준 구분**: 이 정책 변경 이전에 저장된 관망 기록(§2-2)의 지표 스냅샷은
DD 값 자체를 담고 있지 않았다(고가 기준 DD가 있었다면 이번 종가 기준과 다른 값이었을 것이다).
신규 스냅샷에는 계산 기준·버전 식별자(`dd_mdd_calc_basis`, 현재 `"close_v1"`)를 함께 저장해
과거 기록과 구분한다 — 이 키가 없는 스냅샷은 이번 정책 변경 이전(또는 DD·MDD 자체가 계산되지
않은) 기록이라는 뜻이다. 과거 스냅샷을 새 기준으로 재해석하거나 덮어쓰지 않는다.

### 6-3. RSI 특수값 처리

RMA로 계산한 평균 상승폭을 G, 평균 하락폭을 L이라고 한다. 필요한 유효 데이터가 확보됐는지 먼저 검사한다.

- G > 0이고 L = 0이면 RSI = 100.
- G = 0이고 L > 0이면 RSI = 0.
- G = 0이고 L = 0이면 RSI = 50으로 처리하고 “가격 변화 없음”을 표시한다. 이는 우리 서비스의 명시적 계산 규칙이며 외부 차트와의 완전한 일치를 보장하지 않는다.
- G > 0이고 L > 0이면 RSI = 100 - 100 / (1 + G / L).
- 데이터 부족·필수 값 누락이면 RSI는 계산 불가로 반환하고 RSI 매수 조건도 판정하지 않는다. 임의로 0 또는 50을 채우거나 미충족으로 처리하지 않는다.
- 최근 일봉 한 개의 가격 변화가 0이라는 이유만으로 RSI를 50으로 바꾸지 않는다. 특수값 처리는 누적 RMA 평균 G와 L을 기준으로 한다.
- RMA 초기화와 선행 데이터 길이는 §6-4의 확정 기준을 따른다.

### 6-4. RSI RMA 초기화·선행 데이터 기준

- 최초 백테스트 시작일보다 최소 200일 앞선 일봉부터 확보해 계산을 시작한다. 선행 구간은 RSI 초기값 영향을 줄이기 위한 준비 구간이며 투자·성과 집계에는 포함하지 않는다.
- 시간순으로 정렬한 최초 15개 유효 종가에서 14개 변화량을 구한다. 상승폭은 max(변화량, 0), 하락폭은 max(-변화량, 0)으로 두며, 각각 14개 단순평균을 초기 G·L로 사용한다.
- 이후 G = (이전 G × 13 + 새 상승폭) / 14, L = (이전 L × 13 + 새 하락폭) / 14로 갱신한다. RSI 변환과 특수값 처리는 §6-3을 따른다.
- 현재 지표 조회와 백테스트는 같은 초기화 기준·계산 함수·가격 이력을 사용한다. 백테스트에서는 해당 시점까지의 값만 참조한다.
- 매일 또는 매월 비교 구간이 이동해도 RMA 시작점을 옮겨 재초기화하지 않는다. 초기 기준일과 누적 계산 상태를 보존하고, 재계산할 때도 동일한 초기 기준부터 계산한다.
- 계산 상태에는 마지막 처리 일봉과 G·L을 저장해 동일 일봉을 중복 반영하지 않는다. 이전 가격 데이터 정정 시에는 동일한 초기 기준부터 영향 구간을 재계산한다.
- 200일은 서비스의 선행 데이터 설계 기준이며 TradingView와의 완전한 수치 일치를 보장하는 조건은 아니다. 필요한 선행 데이터가 없으면 임의로 기간을 줄이지 않고 계산 불가로 처리한다.

### 6-5. 계산 정밀도와 화면 표시

- 계산·저장에는 원본 입력과 계산 정밀도를 유지한다. 화면 표시를 위한 반올림 값을 후속 계산에 다시 사용하지 않는다.
- 원화 금액·가격은 1원 단위, BTC 수량은 소수점 8자리, RSI·수익률·괴리율·드로다운은 소수점 2자리로 표시한다.
- 매수 조건은 항상 표시용 반올림 전 값으로 판정한다. 예를 들어 RSI 30.004는 30 이하가 아니므로 미충족이다.
- 반올림 때문에 표시값이 임계값과 같아 보이는데 판정이 다른 경우, 필요한 소수 자릿수를 추가 표시하고 반올림 전 값으로 판단했음을 설명한다.
- 평균 매수가·누적 수량·평가금액은 반올림 전 값으로 집계한다. 표시 자릿수 제한을 실제 기록 수량 절삭이나 가상 체결량 변경에 적용하지 않는다.

## 7. 데이터와 분석 범위

- **가격 데이터**: Upbit `KRW-BTC` 48개월 비교 구간과 지표 계산용 선행 일봉. 저장소는 구조화된 로컬 가격 캐시(JSON)로
  한다 — `chroma_db`(벡터 검색)에는 넣지 않는다. 확정 일봉을 날짜순 수치 데이터로 다루므로
  RAG처럼 유사도 검색할 이유가 없고, 지표 계산엔 "최근 200일" 같은 정확한 range 쿼리가 필요해
  벡터 검색과 안 맞는다. `chroma_db`는 `data/docs/*.md` 문서 RAG 전용으로 남긴다.
- **확보 방식**: 최초 1회 페이지네이션 백필(Upbit 일봉 API가 1회 최대 200개까지만 주므로 구간을
  나눠 여러 번 호출) 후, 관련 조회에서 필요한 구간을 증분 수집한다. 09:00은 확정성 경계이며 정각에 자동 실행되는 상주 스케줄러는 없다.
- **저장 기간과 지표 계산 기간을 구분**: 4년치를 보관하되, RSI(14일)·200일 이동평균·1년(365일)
  드로다운 등은 각자 필요한 기간을 사용하고(RSI는 RMA 누적 이력 유지), 장기 흐름 설명에는 4년 전체를 쓴다. 시뮬레이션(§6)의
  "사전 데이터 확보"도 이 캐시에서 조달한다.
- **DD·MDD는 최근 30일·365일·48개월 세 기간**으로 계산해 함께 제공한다(§3-1·§6-2,
  정책 변경 — 종가 기준으로 통일하고 MDD를 새로 추가). 종합 판정표가 폐기된 이후로는 "판정용으로
  어느 기간만 쓸지" 구분이 더 이상 필요 없다 — 세 기간 모두 설명 정보로 동등하게 보여준다.
- **반감기는 지표가 아니라 참고 정보**: 반감기 이후 경과 일수는 배경 설명으로만 표시하고, 판정
  대상에 넣지 않는다 — "반감기가 있으니 같은 가격 패턴이 반복된다"는 전제를 두지 않기 위함이다.
- **투자 기준 문서와 용어집은 교육용으로 직접 정한 기준**이며, Upbit 실시간 시세 같은 외부 사실과
  구분한다.
- **실제 매수·관망 기록은 실제 거래소 주문과 구분한다.** 로컬 파일일 뿐 실제 자금 이동은 없다.

### 7-1. 데이터 확정성·신선도 확인 (기존 §5-1 유지)

**일봉 경계 확정: KST 오전 9시.** Upbit KRW-BTC 일봉은 KST 09:00 기준으로 마감·갱신된다(자정
기준이 아니다). 즉 "확정된 최신 일봉"이 무엇인지는 조회 시각이 오늘 09:00(KST)을 지났는지에 따라
달라진다 — 09:00 이전이면 그 전날 09:00에 마감된 캔들까지만 확정이고, 09:00 이후면 오늘 09:00에
새로 마감된 캔들까지 확정이다. 지표 계산(§3-1, §3의 전략 조건, §6 시뮬레이션 전부)은 **이 기준으로
가장 최근에 마감된 일봉까지만** 쓴다. 아직 마감 전인 캔들을 계산에 섞으면 같은 날 안에서도 물어본
시각에 따라 결과가 달라져 재현성이 깨진다. (`get_btc_price`처럼 실시간 현재가 조회는 이 규칙과
무관하다.)

**확인 순서**:
1. 계산 전, 로컬 가격 캐시의 마지막 확정 일봉이 위 09:00(KST) 경계 기준으로 "지금 시점에 마감돼
   있어야 할 가장 최근 캔들"과 일치하는지 결정적으로 확인한다(임의의 유예 기준을 따로 두지 않는다).
2. 일치하지 않으면(갱신 파이프라인이 밀린 경우) 계산을 바로 진행하지 않고, "최근 데이터가 OO까지만
   확보돼 있어 마감된 최신 일봉이 없습니다. 그래도 진행할까요?"라고 되묻고 멈춘다.
3. 사용자가 진행을 원하면 계산은 하되, 답변에 항상 "⚠ OO일 기준(N일 지난 데이터)" 캐비어트를
   붙인다.

**API 계약 확장**: `/query` 응답에 `data_gap_needs_confirmation` 부가 필드, `QueryRequest`에
`proceed_with_stale_data` 선택 필드(무상태 API라 확인 신호를 재요청으로 실어 보내는 방식).

> **구현 현황**: src/indicators.py에서 RSI RMA·MA200·종가 기준 DD/MDD를 계산한다. src/price_history.py는 확정성·증분 갱신·복구·결측 검사를 담당한다. 지표별 필요한 구간을 독립 검사하며 오래된 데이터 사용 동의로 필수 결측을 우회하지 않는다.

## 8. RAG 문서 구성과 갱신

현재 data/docs/에는 BTC.md, DCA.md, RSI.md, dca_strategy.md, market_indicators.md, backtest_guide.md, service_rules.md, risk_management.md의 8개 문서가 있다. glossary.md는 주제별로 통합했다.

**재구성 방식 확정**:
- **`glossary.md`는 폐지하고 내용을 주제별 파일로 재배치한다** — RSI 정의는 `RSI.md`로, 이동평균·
  드로다운 정의는 `market_indicators.md`로, 반감기·공급 구조는 `BTC.md`로, DCA 정의는 `DCA.md`로
  옮긴다. 하나의 파일에 서로 다른 주제를 모아두면 RAG 검색이 관련 없는 다른 용어까지 함께 끌고
  오기 쉬워서, 주제별로 쪼개는 쪽을 택했다.
- **`dca_strategy.md`는 이 서비스 고유 규칙 전용으로 재작성한다** — 기존의 "저점 판단 기준"(3지표
  종합, §3-1에서 폐기된 개념)은 삭제하고, §3의 세 전략(하락일·정기 분할·RSI)과 그 확정 조건만
  담는다. 일반적인 "DCA란 무엇인가"·"매수 주기 일반론" 부분은 `DCA.md`로 옮긴다.
- **`risk_management.md`는 그대로 유지한다** — 새 7개 문서 계획과 주제가 겹치지 않는다.
- 최종적으로 `data/docs/`는 아래 계획 문서 7개(신규 6개 + 재작성 `dca_strategy.md`)와
  유지하는 `risk_management.md`를 합쳐 총 8개 파일로 정리된다.

**문서별 역할**:
- `BTC.md` — 비트코인·블록체인·공급 구조·반감기·위험 (`glossary.md`의 반감기 정의 흡수)
- `DCA.md` — 정액 분할매수 개념·예시·장점·한계, 매수 주기 일반론 (`glossary.md`의 DCA 정의,
  기존 `dca_strategy.md`의 일반론 부분 흡수. 정기 분할 = 전통적 DCA로 설명)
- `RSI.md` — RSI 개념·계산(RMA)·해석·한계 (`glossary.md`의 RSI 정의 흡수. 이 서비스의 RSI≤30
  매수 규칙과 일반적 "과매도" 설명을 구분해서 서술)
- `market_indicators.md` — 일봉·음봉·MA200 괴리율·기간별 드로다운 (`glossary.md`의 이동평균·
  드로다운 정의 흡수)
- `dca_strategy.md` — 이 서비스의 세 전략과 매수 조건(§3) 전용으로 재작성 (일반론은 위 `DCA.md`로
  이동, 옛 3지표 종합 판단 기준은 삭제)
- `backtest_guide.md` — 시뮬레이션 가정(§6)·결과 해석
- `service_rules.md` — 최초 이용·예산·월별 진행·전략 변경·기록 규칙(§2·§4·§5)

**문서 형식**: 한 줄 설명 / 쉬운 예시 / 자주 묻는 질문 / 해석상 한계 / 출처 URL·확인 날짜.

**원칙**:
- 외부 자료는 원문 전체 복사 대신 자체 요약한다.
- 일반적인 금융 개념(참고 출처의 정의)과 사용자와 합의한 이 서비스의 규칙(§3의 확정 조건 등)을
  항상 구분해서 서술한다 — 예: "정기 분할은 전통적인 DCA, 하락일·RSI 방식은 월 정액 투자에 시점
  조건을 결합한 이 서비스만의 변형"이라고 명시.
- 미확정 정책(§13)은 확정된 사용 안내에 섞지 않는다.
- 설명은 RAG(문서 검색)가 담당하고, 현재 가격·지표·매수 조건·예산·평균 매수가·시뮬레이션은
  결정적 Python 도구가 계산한다 — 복합 질문에는 둘을 함께 쓴다(기존 Day2/Day6 구조 유지).
- 문서 추가 시 인덱스(chroma_db) 갱신과 실제 검색 검증까지 후속 작업에 포함한다(§14).

**참고 출처**:
- BTC: https://bitcoin.org/ko/faq
- DCA: https://www.investor.gov/introduction-investing/investing-basics/glossary/dollar-cost-averaging
- DCA 장단점: https://www.finra.org/investors/insights/dollar-cost-averaging
- RSI: https://kr.tradingview.com/support/solutions/43000502338/

**RSI 계산법**: src/indicators.py의 RMA 계산을 현재 지표와 백테스트에서 공유한다(§6-4).

**구현 현황**: 문서 8종과 Chroma 검색을 사용한다. 문서 내용·청킹 설정·임베딩 모델 지문에 따라 인덱스를 갱신한다. 문서 수정 시 평가 reference와 검색 근거도 확인한다.

## 9. 서비스 확장 관점

**개인 사용자에게 제공하는 가치**: §1에서 설명한 대로, 예산·기록 자동 추적 + 조건부 전략의 결정적
판정 + 과거 데이터 기반 전략 비교 + 판단 근거의 기록.

**규모**: 로그인 없는 개인 1인 사용, MVP 범위. *(실사용 빈도 실측치 없음 — 「기획 가정」)*

**대체제와의 차별점**: 단순 가격 알림봇과 달리 (1) 월 예산·평균 매수가·남은 예산을 자동 추적하고,
(2) 세 전략의 매수 조건을 코드로 결정적으로 판정하며, (3) 전략 변경 전 과거 4년 시뮬레이션으로
비교해볼 수 있고, (4) 투자 현황 집계에 영향을 주는 기록 생성·수정·취소는 항상 승인을
거친다.

**수익화 구상**: 기본 조회·전략 설명은 무료로 제공하고, 심화 분석·맞춤 리포트·기록 내보내기를 월 구독 기능으로 확장한다. 유료 기능은 향후 구상이며, 지불 의사는 추후 검증한다. 이번 MVP에 결제 기능은 포함하지 않는다.

**이후 확장 가능성**: (전부 검증되지 않음 — 「기획 가정」) 같은 예산·전략·승인 게이트 구조를
다른 자산(주식, ETF 등)에도 적용 가능해 보이지만, 이 프로젝트 범위와는 별개다.

## 10. 서비스 정책 (가드레일 요약)

| # | 정책 | 현재 구현·검증 범위 |
|---|---|---|
| 1 | 데이터 부족·지연을 추정으로 채우지 않음 | 확정성·신선도·지표별 필요 구간 검사 구현 |
| 2 | 계산은 코드, 설명은 근거 기반 | 예산·전략·지표·백테스트 구현 |
| 3 | 근거 없는 규칙 설명 금지 | RAG·역할 지침 적용. 모든 자유 서술의 정확성 보장은 아님 |
| 4 | 가격 예측·수익 보장 금지 | 가드·프롬프트 적용, 새로운 표현 검증 필요 |
| 5 | 실제 주문 실행 금지 | 시세 API와 로컬 기록만 사용 |
| 6 | 매수 기록은 승인, 예산·전략은 별도 확인 | 생성·수정·취소·초기화 승인, 예산·전략 확인/취소 구현. 관망은 예외 |
| 7 | 승인 인자 일치·중복 방지 | 서버 인자 보관·Lock·상태 전이와 경합 테스트 |
| 8 | 개인정보 보호 | 마스킹·가드 구현. 답변·trace 모든 경로 완전 보호로 해석하지 않음 |
| 9 | 정책·승인 우회 금지 | 입력 가드·확인 API, 회귀 사례 검증 |

세부 케이스는 `evaluation/test_queries.csv`의 `guardrail` 카테고리에서 검증한다.

### 10-1. 승인 설계 확정 (정책 6·7 상세, 기존 §8-1 유지 + 확장)

**해결한 문제**: 과거 클라이언트 tool/args를 직접 실행하던 구조를 서버 발급 승인 ID 기반으로 교체했다. 현재는 저장된 작업·인자·상태를 검증한다.

**확정한 설계**:
1. **승인 ID 발급**: 승인이 필요한 도구 호출을 발견하면 서버가 `approval_id`를 발급해
   `{tool, args, status: "pending"}`으로 저장하고, 응답에 이 id를 포함한다. `/approve`는 이 id만
   받고, 서버가 저장해둔 tool/args를 그대로 실행한다(클라이언트가 보낸 tool/args는 신뢰하지 않음).
2. **저장 위치**: 서버 프로세스 메모리(dict). 단일 프로세스·개인 1인·저빈도 사용이라 파일/DB
   영속화는 불필요 — 재시작 시 대기 중 승인이 사라지는 건 알려진 한계로 남긴다.
3. **무효화 규칙**: 실행 전 pending → executing 전환을 원자적으로 처리해 동시 승인 중 하나만 실행한다. 실행 결과가 불명확하면 같은 ID로 자동 재실행하지 않고 장부와 상태를 확인한다. 1회용만 — 실행/거부 후 같은 id 재요청은 거부. 시간 만료(TTL)는 이번 범위에
   넣지 않는다.

**적용 대상 확장**: 이번 갱신으로 승인이 필요한 동작이 늘었다 — 이 설계(승인 ID 발급)는 다음 모두에
동일하게 적용한다:
- 매수 기록 생성(기존 `record_virtual_buy`)
- **매수 기록 수정·취소(`amend_virtual_buy`/`cancel_virtual_buy`, §4-1에서 확정)**
- 장부 초기화(`reset_ledger`)
- 관망 기록(`record_watch_decision`)은 승인 불필요(read 취급, §2-2)로 예외 유지

**API 계약 변경**: `approvals_needed` 각 항목에 `approval_id` 추가, `ApproveRequest`를
`{"approval_id": "서버가 발급한 ID"}`로 단순화.

**승인 거절 API (확정)**:
- `POST /reject`, 요청: `{"approval_id": "서버가 발급한 ID"}`.
- 사용자가 대기 중 작업을 거절하면 에이전트가 해당 승인 ID로 거절 처리를 요청한다. 대상이 여러 개이고 사용자의 지시가 모호하면 대상을 확인한다.
- 서버는 `pending → rejected`를 원자적으로 처리한다. 승인 실행과 경합하면 먼저 성립한 상태 전이만 허용한다.
- 성공 응답: `{"approval_id": "...", "status": "rejected"}`. 도메인 도구를 실행하지 않으며 매수 기록을 저장하지 않는다.
- 존재하지 않는 ID는 404, 이미 실행 중·실행 완료·거절된 ID는 409로 거부한다. 거절된 ID로 `/approve`를 요청해도 실행하지 않는다.
- 이미 저장한 매수 기록의 취소는 `cancel_virtual_buy`와 별도 승인 절차를 사용한다. `/reject`는 실행 전 승인 요청만 거절한다.
- 구현 상태: 승인·거절 API와 Lock 기반 전이를 구현했고 개발팀이 경합 테스트 결과를 보고했다.

## 11. 성공 기준과 평가 방법

기존 네 축(편의성·정확성·설명력·통제)을 유지한다. 이번 갱신으로 "정확성" 축에 아래가 추가로
필요하다(세부는 §14 후속 작업):
- 예산·남은 예산·평균 매수가 계산의 고정 데이터 기반 검증
- 세 전략 조건 판정의 고정 데이터 기반 검증
- 시뮬레이션 결과의 재현성 검증(같은 입력 → 같은 출력)

**평가 방법과 결과 범위**:

| 방법 | 대상 | 기록 |
|---|---|---|
| pytest (tests/, ui/tests/) | 계산·경계·상태·승인·UI | 개발팀 최신 보고 236/236, 34/34 |
| 규칙 기반 run_eval.py | 기대 도구·문구·가드 | 과거 Haiku 22문항 22/22 |
| LLM-as-Judge | 답변 품질·일관성 | 과거 Haiku 응답·채점 19/22 |
| RAGAS | 검색·근거·질문 관련성 | 8문항 완료. recall 1.00 / precision 0.77 / faithfulness 0.67 / relevancy 0.55 |
| 사용자 실사용 | UI 핵심 흐름 | TC1·TC2 정상 동작 확인 |

시점·모델·세트가 다른 결과를 합산하지 않는다. 최신 코드 전체 재평가 완료를 뜻하지 않는다.
positive 80% 이상, negative·guardrail 100%, 필수 코드 테스트 전체 통과 목표를 유지한다.
실측과 과거 개발 근거는 [통합 평가 보고서](evaluation/round2_report.md)를 참고한다.

### 11-1. 필수 테스트 완료 기준 (확정)

정상 요청은 80% 이상, negative·guardrail은 100% 통과 목표를 유지한다.
이와 별개로 핵심 계산·시간 경계·승인 처리의 사전에 정한 필수 테스트는 모두 통과해야 완료로 판정한다.
필수 테스트 하나의 실패도 전체 평균 통과율로 상쇄하지 않는다. 나머지 edge 사례도 결과를 공개한다.

필수 검증 범위:
- 월 예산·평균 매수가, 실제 매수 기록 수정·취소 후 재집계.
- RSI 30 및 일봉 −5%의 직전·동일·직후 값, 첫 매수가보다 낮아야 하는 엄격한 비교, 반올림 전 판정.
- KST 09:00 일봉 경계, 15일 09:00 정기 분할 변경 마감, 월 마지막 날 00:00 전략 변경 마감.
- 중복·동시 승인 실행 방지, 거절된 승인 ID의 실행 차단.
- 백테스트 미래 데이터 참조 금지, 월말 조건 매수·의무 매수 중복 방지, 동일 입력의 재현성.

고정 가격·시간·기록 입력과 독립적으로 산출한 기대값으로 결정적 테스트를 수행한다.
단순 도구 호출 여부나 LLM-as-Judge 평가만으로 위 검증을 대체하지 않는다.
평가 세트와 필수 테스트 목록을 먼저 정하고, 라운드별 통과·실패·미실행 결과를 남긴다.
이 절은 완료 목표이며, 테스트 구현 또는 통과 실적을 의미하지 않는다.

## 12. MVP 범위 — 확정

아래 범위를 구현·평가 기준으로 확정한다. 기획 확정은 구현 완료를 의미하지 않으며, 개발 순서와 진행 상태는 §14에서 관리한다.

**포함하기로 확정한 것**:
- 월 예산 설정·추적, 실제 매수 기록(생성·수정·취소)
- 세 가지 매수 방식(하락일·정기 분할·RSI)과 각 조건 판정
- 전략 미선택 상태 관리, 월중 전략 변경, 15일 이후 제약, 월말 처리, 놓친 신호 재계산
- 과거 4년 3전략 백테스트 시뮬레이션
- 관망 기록
- 4년치 가격 히스토리 로컬 캐시 (기존 필수 완료 조건 유지)
- 데이터 확정성·신선도 게이트 (기존 필수 완료 조건 유지)
- 승인 인자 일치·중복 방지, 매수 기록 수정/취소까지 승인 확장 (기존 필수 완료 조건 유지)
- 주제별 RAG 문서 8종·인덱스 갱신
- Streamlit 채팅·확인/취소·승인/거절·지정된 후속 입력
- 월중 시작·시작월 앞당기기·종가 기준 DD/MDD

**제외 (기존 유지)**:
- 로그인·사용자별 장부 분리·전체 대화 메모리
- 실제 매수·매도 주문
- 미래 가격·저점 확정 예측 또는 수익 보장
- 여러 코인 지원 (BTC 전용)
- 자유로운 투자 전략 생성·최적화 (전략은 3개 고정)
- 자동 정기매매(알림 없이 질문 시에만 응답)
- 과거 성과 기반 전략 자동 선택

## 13. 세부 검토 결과·외부 확인 사항

주요 서비스 정책과 이번 검토 대상 세부사항을 확정했다. 구현·테스트 완료 여부는 별도로 관리하며, 외부 확인이 필요했던 사항은 모두 §13-1로 이관해 확정했다(§13-2 참고).

### 13-1. 검토 완료 사항

1. **승인 거부 인터페이스 — 확정**: `POST /reject`에 `approval_id`를 전달해 대기 중 승인을 거절한다. 상세 계약은 §10-1을 따른다.
2. **계산 세부 계약**: DD·MDD의 기준(종가)과 기간(30일·365일·최근 48개월)은 확정(§6-2,
   정책 변경 — 기존 고가 기준 DD를 종가 기준 DD·MDD로 통일). RSI 특수값 처리는 확정(§6-3). RMA
   초기화·최소 200일 선행 데이터는 확정(§6-4). 계산 정밀도·화면 표시는 확정(§6-5).
3. **평가 완료 기준**: 기존 positive 80% 이상, negative·guardrail 100% 목표는 유지한다. 핵심 계산·시간 경계·승인 처리의 필수 테스트는 전부 통과해야 완료로 판정한다(§11-1).
4. **Day10 제출 마감 시각 — 확정**: 사용자가 최종 제출 마감을 **15:00**으로 명시했다. 이전 14:00 표기는 사용하지 않는다.
5. **평가 파일 형식 — 확정**: 제출 양식·평가기 모두 `evaluation/test_queries.csv` 형식이 맞다(`test_queries.json`이 아니다). 변환·형식 변경이 필요 없다.

### 13-2. 외부 확인 사항

없음 — 남아 있던 두 건(제출 마감 시각, 평가 파일 형식) 모두 §13-1로 이관해 확정했다.

## 14. 구현 현황과 후속 검증

### 14-0. 도구와 확인 경로

| 도구명 | 역할 | 위험도 |
|---|---|---|
| `get_btc_price` | 실시간 현재가 조회 (기존 유지) | read |
| `get_indicators` | RSI·MA200 괴리율·기간별 DD·MDD **값과 의미 설명**(§3-1, 종합 등급 없음, 정책 변경 — DD·MDD 종가 기준 통일) | read |
| `get_month_status` | 이번 달 예산·선택 전략·진행 상황·남은 예산 조회(§2, §4) — 신규 | read |
| `request_monthly_budget_amount` | 월 예산 금액을 물어야 할 때 후속 입력 상태(§2-3)를 발급하는 무인자·무부작용 신호 도구 — 신규 | read(아무 상태도 바꾸지 않는 순수 신호 — RISK_LEVELS 등록을 빠뜨리면 미등록 도구 기본값 때문에 승인 대기로 잘못 멈춘다, 실제로 한 번 그렇게 됐었다) |
| `set_monthly_budget` | 월 예산 설정/변경/앞당기기(§2-0, §4, §4-2) 제안. 현재 계획 상태를 보고 `initial`(최초 설정 — 이번 달/다음 달 중 선택)/`advance`(아직 시작 전인 계획을 이번 달로 앞당김)/`change`(이미 시작된 계획 변경 — 항상 다음 달부터) 중 하나를 자동으로 고른다. 호출만으로는 반영되지 않고, `/confirm_budget_change`로 확인해야 실제 저장된다(2026-09-18 확정 — 이전엔 즉시 반영이었음, §4-2). `requested_year`/`requested_month`(추가, 정책 변경)로 사용자가 명시한 월을 받아, 실제 적용월과 다르면 그 사실을 구조화된 안내에 담는다(§4-2 "요청 월과 적용월이 다를 때") | read(금액·적용월을 명시한 사용자 확인 후 반영, 승인ID 대상과 구분 — select_strategy와 같은 분류 이유) |
| `select_strategy` | 이번 달 남은 예산에 적용할 전략 선택/변경(§2, §5) — 신규 | read(제안 도구; 전략·적용 월은 별도 확인 API로 반영) |
| `evaluate_current_condition` | 선택 전략의 과거 신호·최신 확정 조건 재계산 | read |
| `run_backtest` | 과거 48개월 3전략 시뮬레이션(§6) — 신규 | read |
| `retrieve_docs` | RAG 문서 검색 (기존 유지) | read |
| `search_ledger` | 매수·관망 기록 조회 (기존 유지) | read |
| `record_virtual_buy` | 실제 매수 기록 생성 (기존 유지, §4-1 스키마 확장) | write(승인ID) |
| `request_buy_execution_detail` | 실제 매수 보고에 체결 가격·수량이 모두 빠졌을 때 후속 입력 상태(§2-3의 `buy_execution_detail` kind)를 발급하는 무인자성(amount_krw/executed_date만 받고 아무 것도 기록하지 않음)·무부작용 신호 도구 — 신규 | read(아무 상태도 바꾸지 않는 순수 신호 — `request_monthly_budget_amount`와 같은 이유로 RISK_LEVELS 등록 필수) |
| `amend_virtual_buy` | 매수 기록 수정 (§4-1 — 신규) | write(승인ID) |
| `cancel_virtual_buy` | 매수 기록 취소 (§4-1 — 신규) | write(승인ID) |
| `record_watch_decision` | 관망 기록 (§2-2 — 신규) | read(승인 불필요) |
| `reset_ledger` | 장부 초기화 (기존 유지) | destructive(승인ID+이중확인) |

`assess_dca_signal`은 폐기한다. 지표 조회는 `get_indicators`가 담당하고, 선택 전략의 조건 판정은 별도의 결정적 전략 함수로 구현해 월 상태 조회·응답에 연결한다. `select_strategy`의 사용자 확인은 서버에서 검증하며, 일반 write 승인 분류에 의존해 생략하지 않는다. 월 예산 설정·다음 달 변경 예약도 월 상태 엔진의 필수 인터페이스로 구현한다.

### 14-1. 현재 구현 위치

| 영역 | 구현 위치 | 상태 |
|---|---|---|
| 네 전문 Agent·라우팅·응답 조립 | src/agent.py | 구현됨. 예산 제안은 구조화 안내로 대체, UI narrative 제공 |
| 월 계획·시작월·전략 | src/month_state.py, src/strategy.py | 이번 달/다음 달 시작·앞당기기·변경 확인 구현 |
| 매수·관망 기록 | src/ledger.py, src/agent.py | 기록 승인·수정·취소·집계, 체결 정보 후속 입력 구현 |
| 승인 처리 | src/approvals.py, app.py | 서버 인자 보관·Lock·승인/거절·중복 방지 구현 |
| 일봉·지표 | src/price_history.py, src/indicators.py | 확정성·증분 갱신·필요 구간 검사, RMA·DD·MDD 구현 |
| 백테스트 | src/backtest.py | 완료된 48개월 세 전략 비교 구현 |
| 문서 검색 | src/retriever.py, data/docs/ | 8개 문서·내용/설정/모델 지문 기반 인덱스 갱신 |
| 사용자 화면 | ui/streamlit_app.py, ui/api_client.py | 채팅·확인/취소·승인/거절·신선도 동의·디버그 표시 |
| 평가 | tests/, ui/tests/, evaluation/ | 자동 테스트·규칙 평가·LLM-as-Judge·RAGAS·실사용 기록 |

기본 응답 모델은 `global.anthropic.claude-haiku-4-5-20251001-v1:0`이다. 모델별 결과를 혼합하지 않는다.
현재 trace는 Agent/API 응답 경로에서 제공한다. 별도 `observability.py` 파일의 존재만으로 전체 실행 관측 연동을 완료했다고 주장하지 않는다.

### 14-2. API·UI 연결 요약

| API | 요청 |
|---|---|
| POST /query | question, proceed_with_stale_data=false, awaiting_input_token="" |
| POST /confirm_budget_change, /cancel_budget_change | confirmation_token |
| POST /confirm_strategy_change, /cancel_strategy_change | confirmation_token |
| POST /approve, /reject | approval_id |
| GET /health | 서버 생존 확인; 데이터 디렉터리 식별 기능은 없음 |

응답은 answer·UI 설명용 narrative·contexts·trace·agents_used 및 필요한 경우 approvals_needed,
budget_change_needs_confirmation, strategy_change_needs_confirmation, data_gap_needs_confirmation,
awaiting_input을 포함한다. UI는 narrative와 구조화 카드로 표시하고, 토큰은 내부에서 전달한다.
후속 입력은 monthly_budget_amount와 buy_execution_detail 두 종류이며 전체 대화 메모리는 아니다.

### 14-3. 검증 현황과 남은 작업

- 개발팀 최신 보고: 백엔드 **236/236**, UI **34/34** 통과. 이번 문서 동기화에서 재실행하지 않았다.
- 사용자 TC1(예산→전략→기록 승인→집계), TC2(RSI·기간별 DD/MDD) 정상 동작 확인.
- 개발팀 Haiku 사용액 조회 재검증에서 장부·월 상태 파일 해시 불변 확인.
- 과거 평가: 규칙 기반 22/22, LLM-as-Judge 19/22, RAGAS 8문항 기준선.
  현재 모든 변경을 반영한 전체 평가 결과가 아니다.
- 월말 전액 안내의 실제 모델 검증, 새로운 표현과 복합 질문, 취소·오류 경로의 수동 증거,
  RAG 근거 충실도·질문 관련성 개선은 계속 확인한다.
- 승인·확인·후속 입력은 서버 메모리 상태로 재시작 시 소실한다. 로그인·사용자별 장부 분리·
  여러 프로세스 간 상태 공유는 제공하지 않는다.
- BTC_AGENT_DATA_DIR는 장부·월 상태·일봉 캐시에 적용되지만 src/tools.py의
  price_cache.json에는 적용되지 않는 예외가 있다. 테스트 격리의 남은 보완 사항이다.
- 키워드·정규식과 LLM 프롬프트가 모든 자연어 변형의 정확성을 보장하지는 않는다.

실행은 [README.md](README.md), UI 조작은 [UI 가이드](ui/UI_GUIDE.md),
실사용·과거 개발 평가 통합 근거는 [round2_report.md](evaluation/round2_report.md)를 참고한다.
기존 개발 round3·4·5는 위 보고서의 상세 부록으로 통합했다.

### 문서 동기화 기록 (2026-09-18)

현재 코드·사용자 확정 정책·개발 완료 보고·평가 문서를 대조해 실행 구조와 구현 상태를 갱신했다.
이전 문서의 미래 날짜 표기는 확인되지 않은 개발 메모 날짜로, 이번 개정에서는 현행 정책 설명에서 제거했다.
구현 여부 확인과 테스트 실측은 구분하며, 새 테스트 통과나 외부 배포 완료를 주장하지 않는다.
